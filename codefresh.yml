version: '1.0'
# see https://hub.docker.com/r/progrium/stress/
steps:
  stress:
    type: build
    image_name: progrium/stress
    commands: 
      - --cpu 
      - ${{CPUS}}
      - --vm 
      - ${{MEM256}}
      - --timeout
      - ${{TIMEOUT}}
